# CV

Curriculum Vitae in LaTex

## Build

Install build deps:
```
sudo apt install -y \
  texlive-latex-base \
  texlive-latex-recommended \
  texlive-latex-extra \
  texlive-fonts-extra \
  ghostscript
```

To create the final PDF run:

```
make
```
